export class Post {
  title: string;
  description: string;
  offer: string;

  constructor(title?: string, description?: string, offer?: string) {
    this.title = '';
    this.description = '';
    this.offer = '';
  }
}
